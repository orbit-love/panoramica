mutation ($userId: ID!, $activityId: ID!) {
  updateUsers(
    where: { id: $userId }
    disconnect: { bookmarks: { where: { node: { id: $activityId } } } }
  ) {
    info {
      nodesDeleted
    }
  }
}
