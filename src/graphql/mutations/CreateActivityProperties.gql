mutation SetActivityProperties(
  $id: ID!
  $properties: [ActivityPropertiesCreateFieldInput!]!
) {
  updateActivities(where: { id: $id }, create: { properties: $properties }) {
    activities {
      properties {
        name
        type
        value
      }
    }
  }
}
