mutation DeleteActivityProperties(
  $id: ID!
  $where: ActivityPropertiesConnectionWhere
) {
  updateActivities(
    where: { id: $id }
    delete: { properties: { where: $where } }
  ) {
    info {
      nodesDeleted
    }
  }
}
