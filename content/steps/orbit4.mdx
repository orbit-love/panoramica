import EmptyLayout from "components/mdx/empty_layout";

Explorers are newcomers and passive observers who are primarily interested in getting information and learning. The number of Explorers tends to be large, so it's important to identify the most promising members and start building a relationship.

export default ({ children }) => <EmptyLayout>{children}</EmptyLayout>;
