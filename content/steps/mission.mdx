import EmptyLayout from "components/mdx/empty_layout";

## The Mission

The mission is the community's _center of gravity_.
Members are attracted to the community by its vision for
the future, its past accomplishments, and shared values.
The mission's gravity is a function of how well members understand
it and contribute.

export default ({ children }) => <EmptyLayout>{children}</EmptyLayout>;
