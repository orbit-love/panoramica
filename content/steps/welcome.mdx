import EmptyLayout from "components/mdx/empty_layout";
import c from "lib/common";

## This is a community.

In the Orbit Model, a community is a network with a mission.
The network and mission act as sources of gravity, pulling
members into orbit and holding them there.
Members orbit at different levels according
to their responsibilities and relationships.

export default ({ children }) => <EmptyLayout>{children}</EmptyLayout>;
