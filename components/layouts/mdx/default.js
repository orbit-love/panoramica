import React from "react";

// the default layout is empty, choose the page layout
// for pages; for chunks, surround them with a prose class
export default function Layout({ children }) {
  return <>{children}</>;
}
